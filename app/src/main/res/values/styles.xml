<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Kit.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarTheme">@style/ActionBar</item>
        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="ActionBar" parent="Kit">
        <item name="android:background">@drawable/drawable_gradient</item>
    </style>
    <style name="ButtonStyle" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_round_gradi</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:elevation">2dp</item>
        <item name="android:textColor">#FAFAFA</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
